# 2026-05-25

- Removed the glass effect ("liquid-glass") from the Admin Panel by redefining `.liquid-glass` to be solid `#FFFFFF` in light mode and solid `#141210` in dark mode, removing backdrop filters and blur effects.
- Made the Leads table and details drawer fully functional in `js/admin/components/leads.js`. Added event listeners for:
  - Select all header checkbox (`#leads-select-all`)
  - Row select checkboxes (`.lead-select-cell`)
  - Status modal toggle (`.lead-status-modal-btn`)
  - Single row deletion button (`.delete-lead-btn`)
  - Detail drawer delete button (`#delete-lead-drawer-btn`)
- Integrated Supabase Realtime update channel in `openLeadDrawer` to sync chat messages in real time when active chat details are viewed.
- Added live Supabase Realtime messaging sync to the Technical Support chat interface (`js/admin/components/support.js`) so messages between client and admin reflect dynamically without manual refresh.
- Added cleanup hooks in the Support component to properly unsubscribe from Supabase Realtime channels when switching chats, tabs, or leaving the page view.
- Verified syntax on modified JavaScript files with Node and successfully ran `pack_frontend.ps1` to produce the optimized `deploy.zip` release archive.
- Redesigned the map overlay blur to use a high-quality "liquid glass" frosted glassmorphic design (`backdrop-filter: blur(5px)`) using a pseudo-element on the map container wrapper, and upgraded the interaction badge to match the theme-aware glass look.
- Removed the liquid glass design (transparency and blur effects) from the status change and user role change modals, replacing them with solid opaque backgrounds for improved readability.
- Color-coded leads and appeals in the admin panel table (and mobile cards) matching the color scheme of orders based on their status (blue for new, yellow for in progress, pink for waiting client, green for success, red for cancelled).
- Unified chat styles between admin technical support, lead details, order details, and user support chat, establishing standard rounded-3xl message bubbles, color tokens, and participant labels with icons.
- Resolved contrast and readability issues in the admin panel under the light theme by loading `/js/theme.js` in `admin.html`, adding custom CSS variables, and replacing hardcoded dark-theme colors with Tailwind semantic variables (`text-on-surface`, `text-on-surface-variant`, `bg-surface-variant`, etc.) across `analytics.js`, `orders.js`, `leads.js`, `users.js`, and `drawer.js`.
- Made floating unread chat push notification badges (`floatingChatBadgeGlobal`) and inline unread option badges inside custom selects larger and set their background to theme dark pink (`#ca7093`) with readable high-contrast white text (`text-white`) instead of dark black text (`text-[#0f0e0c]`).
- Restored the left chat sidebar list to the client chat drawer while keeping the top dropdown select and category tabs, synchronizing category selections and active chat state between the sidebar and the dropdown triggers.
- Added a category tab filter and custom-select dropdown selector to the admin technical support workspace (`js/admin/components/support.js`), mirroring the client's dual-select layout.
- Configured topics lists (both client-side and admin-side) to always sort chats with unread messages to the top first, followed by order of last-message timestamp (new to old).
- Fixed the microsecond layout flash/shift when closing the global chat modal by refactoring `lockScrollGlobal` and `unlockScrollGlobal` to avoid resetting scroll position via `position: fixed` and `scrollTo`, replacing it with a padding-compensated `overflow: hidden` layout lock.
- Implemented visual highlight effects (smooth fading green background for "in stock" and red for "out of stock") on product table rows and mobile cards when status changes.
- Added live preview status badge pulse animations (ripple effect) inside the admin product edit/create drawer.
- Implemented toast push notifications (`window.showToast`) for status updates: individual notifications for up to 3 updated products, and a combined summary notification for bulk status updates (more than 3 products).
- Implemented bulk action cancellation and hide behavior on view switches, tab changes, and when opening any details drawer/modal (product, order, lead, user modals, and global chat).
- Enlarged catalog table checkboxes to w-6 h-6 and made the entire left select cell fully clickable for selection on both desktop rows and mobile cards.
- Configured stock status badges ("В наличии" / "Нет в наличии") inside desktop rows and mobile cards to be clickable, allowing direct toggle of product availability status with a loading indicator.
- Upgraded edit and delete buttons in the tables and drawers with premium scaling, backdrop shadow, and icon rotation micro-animations on hover.
- Verified JavaScript file syntax with Node.js and successfully ran `pack_frontend.ps1` to produce the optimized `deploy.zip` release archive.
- Implemented a premium, theme-aware product details card popup modal `window.openProductCardModal(productId)` inside `/js/shared-ui.js` that fetches details dynamically and supports both light/dark themes.
- Rendered order composition items inside the admin order details drawer modal (`/js/admin/components/orders.js`) along with a summary total price.
- Added interactive click handlers to order items across the system: inside the admin orders table & drawer modal, client-side cabinet order list & details modal (`/cabinet.html`), and admin dashboard active orders & revenue report widgets (`/js/admin/components/dashboard.js`), allowing direct preview of any ordered product in a popup.
- Packaged the updated static frontend resources using `pack_frontend.ps1`.
- Reorganized the admin panel sidebar to render nested submenus for "Товары" (Nomenclature, L1, L2, Parameters) and "Техническая поддержка" (Chats, Appeals), hiding the page-level tabs and defaulting views to the first sub-item on parent menu expansions.
- Simplified the specialist support welcome workspace in `js/admin/components/support.js` to render the title "Рабочее место специалиста", the description "Панель управления обращениями и чатами поддержки клиентов", and only the "Найти обращение" button (linked to switch views to the appeals submenu list).
