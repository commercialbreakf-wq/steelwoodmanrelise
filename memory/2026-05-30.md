# 2026-05-30

- Completed the integration of the premium single-line capsule-shaped logo button (`logo-link-capsule`) for the light theme version.
- Fixed a rendering bug where global responsive media queries (`#globalHeader .logo-img-container img`) were stretching the logo image, and a homepage image filter (`html:not(.dark) section:first-of-type img`) was applying a heavy blur to the logo.
- Overrode selectors using high-specificity classes (`#globalHeader .logo-link-capsule .logo-img`) combined with `!important` to lock the logo image size strictly to `36px` and apply `filter: none` to keep the shield sharp.
- Handled the browser caching issue: developed a node script that incremented the `shared-ui.js` query string version from `?v=19` to `?v=21` across exactly 34 HTML files, ensuring that browsers bypass the local cache and immediately render the correct light theme capsule styling.
- Upgraded the light mode border opacity and width (`border: 1px solid rgba(202, 112, 147, 0.5)`) to stand out elegantly against the translucent glass-like off-white background.
