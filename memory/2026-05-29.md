# 2026-05-29

- Adapted all custom dropdown lists in `catalog.html` (Sizes, Lengths, Steel Types, and Sorting dropdowns) for the light theme version.
- Replaced hardcoded dark background (`bg-[#1a1816]`) and dark border properties on the dropdown containers with a centralized, premium `custom-dropdown-menu` class.
- Replaced hardcoded hover highlight styles (`hover:bg-white/5`) on the dropdown items with a centralized `custom-dropdown-item` class.
- Added premium adaptive styling rules for `.custom-dropdown-menu` and `.custom-dropdown-item` to `js/theme.js`:
  - **Dark Mode (Default)**: retains a rich dark background (`#1a1816`), elegant subtle borders (`rgba(255,255,255,0.15)`), and a dark-grey hover highlight (`rgba(255,255,255,0.05)`).
  - **Light Mode**: displays a clean premium solid white card background (`#ffffff`), soft outline border (`rgba(0,0,0,0.08)`), dark text color (`#1A1817`), a modern shadow (`0 10px 30px rgba(0,0,0,0.08)`), and a gorgeous soft pink hover highlight (`rgba(202, 112, 147, 0.08)`) that matches the dusty rose theme.
- Fixed checkbox visibility in light mode: implemented general CSS rules inside `js/theme.js` to ensure the unselected checkbox borders (using the `border-outline-variant` class) adapt from semi-translucent white (`rgba(255,255,255,0.25)`) in dark mode to clean, visible charcoal (`rgba(0, 0, 0, 0.15)`) in light mode, preventing them from appearing invisible on the light card background.
- Aligned dropdown labels and titles (`text-on-surface-variant`) inside the menus to remain beautifully readable, using soft medium-gray text (`rgba(83, 77, 74, 0.7)`) in light mode.
- Made the theme toggler (sun icon) beautifully pink (`var(--color-primary)`) when light mode is active to perfectly match the nearby pink action/header buttons, adding styling rules across `js/theme.js` and `js/shared-ui.js` (including both admin and client styling blocks).
- Adapted the Technical Support Chat in the Admin Panel (`js/admin/components/support.js`) to implement identical unread chat logic and styling as the global client-side support chat drawer (`js/shared-ui.js`):
  - Replaced the simple last-message sender check with precise `localStorage` unread count tracking (`metal_chat_read_times`) that calculates the number of new client messages received since the chat was last read.
  - Aligned state tracking so that reading/opening a chat, sending a message, or receiving a new message via Supabase Realtime in the admin view updates the chat read times in real-time, syncing the state perfectly between the admin support panel and the global chat drawer.
  - Grouped and rendered active chats into distinct `НЕПРОЧИТАННЫЕ` (Unread) and `ПРОЧИТАННЫЕ` (Read) sections without duplication, complete with elegant headers, pink item counts, and animated pink pulse dots.
  - Implemented the dusty rose color palette (`#ca7093`) and hover styles matching the custom premium theme of the support messenger interface.
- Integrated the "Открыть" (Open) button inside the Admin Support Chat (`js/admin/components/support.js`) with the "Клиенты" (Users) profile modal component:
  - Exported `openUserModal` from `js/admin/components/users.js` for external access.
  - Linked the "Открыть" button in `support.js` to automatically fetch user records from state, match the active chat client by email or phone, and open their complete profile information pop-up (including company details, legal/actual addresses, active order list, and SMS chats) directly on top of the technical support workspace.
- Implemented clickable order items in the user profile modal:
  - In the "Заказы" (Orders) tab of the user profile modal ([users.js](file:///c:/Users/egas1/Downloads/Stitch_first%20%E2%80%94%20%D0%BA%D0%BE%D0%BF%D0%B8%D1%8F%20%283%29%20%E2%80%94%20%D0%BA%D0%BE%D0%BF%D0%B8%D1%8F/js/admin/components/users.js)), transformed the static order composition text list into clickable inline-block badges styled to fit the dusty rose theme.
  - Clicking a badge triggers `window.openProductCardModal(productId)`, dynamically opening the premium product details popup, while stopping event propagation to avoid triggering the parent order drawer.
  - In the order details popup (`openOrderFromUserModal`), updated the order composition chips under "Состав заказа" to be fully interactive hoverable items that open their respective product details.
  - Polished the dynamic text color of the chat tab's order select dropdown to update instantly when a new option is chosen.
- Made order composition product row containers clickable:
  - In `js/admin/components/orders.js`, modified both desktop table, mobile view, and order drawer detail popup templates so that the product modal popup click handler (`window.openProductCardModal(productId)`) is bound directly to the entire product item container row instead of only the product name.
  - Styled these container rows with `hover:bg-primary/5 hover:border-primary/30 cursor-pointer transition-all` to clearly indicate interactive states.
- Styled the theme switcher (sun/moon toggler button) in the global header to look exactly like the adjacent circular buttons (Search, Cart, Profile):
  - Modified the HTML markup in `js/shared-ui.js` and `js/shared-ui-copy.js` to add classes `text-on-surface hover:text-primary transition-all duration-300 p-2 rounded-full hover:bg-white/5 active:scale-95`.
  - Cleaned up the CSS properties for `.theme-toggle-btn` to use `background-color: transparent` and `border: 1px solid transparent` instead of overriding backgrounds and borders entirely.
  - Injected light mode rules for `#globalHeader .theme-toggle-btn` in `js/theme.js`, `js/shared-ui.js`, and `js/shared-ui-copy.js` to apply the exact soft circular grey-ish background (`rgba(0, 0, 0, 0.03)`), borders (`1px solid rgba(255, 255, 255, 0.5)`), and shadows (`box-shadow: 0 15px 50px rgba(0, 0, 0, 0.03), inset 0 0 30px rgba(255, 255, 255, 0.4)`) to match the adjacent buttons.
- Removed `#` prefix formatting across all customer cabinet views (`cabinet.html`), cart views (`cart.html`), shared support files (`js/shared-ui.js`, `js/shared-ui-copy.js`), and all admin component views (orders, users, leads, support, analytics, dashboard, state helper) to display clean numeric IDs.
- Extended the backend PUT `/api/admin/orders/:id` endpoint to parse and update the `kp_url` field.
- Split the invoice link block into separate "Коммерческое предложение" (`kp_url`) and "Счет на оплату" (`invoice_url`) input fields inside the Admin Order drawer and User Profile Order modal.
- Added a file upload feature enabling administrators to upload PDF files directly. Clicking the upload icon converts the PDF to Base64, sends it to a new backend `/api/admin/upload-file` route which saves the file in the `/uploads/` directory, and populates the text field with the static URL.
- Handled UI button visibility in the Customer Cabinet (`cabinet.html`):
  - When the order status is "Ожидание" (`waiting_client`), show both "Коммерческое предложение" and "Счет на оплату" buttons. If files are not yet uploaded, display a helpful message indicating that the document is being prepared.
  - If the status is not `waiting_client` but either of the URLs are set, render the respective download links.
- Updated the Client Cabinet order details modal layout (`cabinet.html`):
  - Renders the order total price at the top-right of the modal header (aligned beautifully next to the close button).
  - Configured each item card in the order composition list to be fully clickable, redirecting the user to the product modal card popup.
- Resolved the `SyntaxError: Unexpected reserved word` in `js/admin/app.js` by closing the unclosed `btn.onclick` tab handler.
- Completed the subpage routing implementation in the Admin Panel by importing and mapping `renderParametersView` for `#products_parameters`, styling the tabs dynamically, synchronizing clicks with the URL hash, adapting tab context actions (filters display, button text), and setting up subscription cleanup to prevent memory leaks.
- Ran an automated Cheerio/Axios parser script `scratch/parse_armatura.js` to successfully extract all 44 unique products from the "Арматура" category of `metallobazav.ru` catalog with complete formatting and saved results to `armatura_products.json` and `armatura_products.md`.
- Integrated the premium "Volhonka"-styled purchase box into the standalone product details page `product.html` specifically when an Armature product is loaded, supporting dynamic radio price toggles (rod, meter, ton), real-time sum calculator, and smooth add-to-cart actions with fallback/standard product UI support.
- Fixed vertical misalignment in `catalog.html` between top filters (e.g. `ДЛИНА: ЛЮБАЯ` dropdown button) and action buttons (`ПРИМЕНИТЬ` / `СБРОСИТЬ`) by standardizing all button and dropdown heights to exactly `h-[46px]` and adding flex centering styles.
- Resolved "ТОВАР НЕ НАЙДЕН" crashes on the product detail page (`product.html`) by introducing robust translit and slugified name/id matching inside the single product fetch API (`/api/products/:id`) in `api/[...slug].js`.
- Fixed TypeError crashes in the product quick view modal in `js/shared-ui.js` and `js/shared-ui-copy.js` by safely converting product length, weight, and specs parameters to `String` before applying the regex `.match()` parser, and synchronised specifications normalization to handle standard Supabase cache array format.

