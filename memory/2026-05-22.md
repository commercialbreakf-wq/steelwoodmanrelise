# 2026-05-22

- Выполнил смещение левого блока с хлебными крошками вниз на странице товара (`product.html`).
- Хлебные крошки (`#breadcrumbs`) перенесены из верхнего общего ряда CSS Grid внутрь левой колонки над изображением товара.
- Верхний край левой колонки (хлебные крошки) теперь идеально совпадает по вертикали с верхним краем правой колонки (категория и название товара), обеспечивая выравнивание по верху экрана.
- Картинка товара сместилась ниже, освободив место для хлебных крошек.
- Адаптировал страницу товара под мобильную версию, чтобы картинка, цена и параметры заказа были на главном экране при загрузке:
  - Уменьшил отступы сверху страницы и грида на мобильных.
  - Изменил соотношение сторон картинки на `21/9` на мобильных, чтобы сделать ее компактнее по высоте.
  - Переверстал блок цены на мобильных в горизонтальный формат (одна строка).
  - Сжал внутренние отступы и размеры шрифтов в калькуляторе параметров.
  - Оптимизировал размер названия товара до `text-[22px]`.
- Обновил файл `walkthrough.md` для документирования этих изменений.
- Выполнил оптимизацию мобильных отступов и контейнера калькулятора на странице товара (`product.html`):
  - Перенес хлебные крошки в верх первой колонки, чтобы они отображались на мобильных в самом верху над картинкой.
  - Сохранил без изменений размеры картинки, заголовка и цены.
  - Устранил огромные пустоты и пробелы выше и ниже калькулятора на мобильном (уменьшил отступ Specs & Description секции до `mt-4 pt-4` и Related Products до `mt-8`).
  - Оформил контейнер параметров заказа в виде премиальной солидной карточки (как на ПК): задал отступы `p-6` вместо `py-3 px-4`, скругление углов `rounded-3xl` вместо `rounded-2xl` и внутреннее расстояние `space-y-5 md:space-y-8`.
- Устранил растягивание и лишнее пустое пространство под кнопкой «В КОРЗИНУ» в калькуляторе на мобильных экранах:
  - Добавил класс `no-full-height` к контейнеру калькулятора (`section.glass-panel`), а также к секциям описания, характеристик и похожих товаров в [product.html](file:///c:/Users/egas1/Downloads/Stitch_first/product.html). Это отменило принудительное растягивание секций на `100dvh` на мобильных экранах.
  - Для предотвращения аналогичных багов добавил класс `no-full-height` к категории в [catalog.html](file:///c:/Users/egas1/Downloads/Stitch_first/catalog.html), разделу редактирования профиля в [cabinet.html](file:///c:/Users/egas1/Downloads/Stitch_first/cabinet.html) и CTA-блоку в [contacts.html](file:///c:/Users/egas1/Downloads/Stitch_first/contacts.html).
- Расположил кнопку «ВЕРНУТЬСЯ В КАТАЛОГ» над хлебными крошками (в левой колонке над изображением товара) в [product.html](file:///c:/Users/egas1/Downloads/Stitch_first/product.html), объединив их в отдельный навигационный контейнер для улучшения визуальной иерархии и соблюдения принципа близости (UI/UX Senior).
- Исправил недостаточный контраст текста в выпадающем меню каталога (mega menu) при активной светлой теме:
  - Изменил цвет заголовка по умолчанию («Каталог продукции» / `.mega-default-title`) на темно-серый/черный (в соответствии с основным цветом текста темы: `#151311` в `shared-ui.js` и `#3B3B3B` в `shared-ui-copy.js`).
  - Изменил цвет описания под ним («Наведите на категорию, чтобы увидеть подкатегории» / `.mega-default-desc`) на нейтральный серый (`#6b7280` / `#827D7E`).
- Исправил баг с пропаданием правого блока выпадающего меню каталога (mega menu):
  - Добавил принудительный сброс активных классов пунктов меню и подменю при открытии каталога (`openMegaMenu`), чтобы меню всегда стартовало с дефолтного состояния.
  - Исправил обработчик `mouseenter` для пунктов меню: при наведении на разделы без заданного подменю (`data-submenu` возвращает `undefined` или `none`, такие как «История и цели», «Автопарк», «Сертификаты», «Контакты») теперь гарантированно отображается дефолтный правый блок («Показать все товары»).
- Стилизовал и адаптировал выпадающие списки (custom selects) согласно новым требованиям дизайна:
  - Уменьшил высоту кнопок-триггеров и элементов опций на 30%, скорректировав внутренние отступы (padding с `7px 14px` до `5px 12px`, и с `10px 16px` до `7px 12px` для крупных селектов).
  - Сделал цвет фона выпадающих списков и триггеров в светлой теме светло-серым (`#ebebeb`) с тонкой рамкой в соответствии со скриншотом пользователя.
  - Добавил принудительное свойство `white-space: nowrap` для кнопок-триггеров выпадающих списков, гарантируя отображение дефолтной сортировки («СОРТИРОВКА: ПО УМОЛЧАНИЮ») и других длинных пунктов строго в одну строку без переносов.
- Исправил контрастность текста подкатегорий в каталоге на темной теме:
  - Установил цвет текста для активного пункта подкатегории на светло-серый (`#e7e2dd`) вместо темно-бордового (`#964551`), что обеспечило хорошую читаемость на фоне `rgba(150, 69, 81, 0.15)`.
  - Изменил цвет текста при наведении (hover) на фирменный винно-красный (`#964551`), а фон при наведении сделал полупрозрачным (`rgba(150, 69, 81, 0.08)`) вместо сплошной заливки, чтобы текст оставался идеально читаемым.
- Настроил блокировку скролла/зума карт Yandex Maps при прокрутке страницы:
  - Добавил класс `.map-container-wrapper` к контейнерам `<iframe>` карт в `about.html`, `contacts.html` и `logistics.html`.
  - Внедрил в `js/shared-ui.js` динамические стили (блокирующие `pointer-events: none` по умолчанию и возвращающие их при `.active`), а также скрипт активации карты по клику и деактивации по `mouseleave`/клику вне карты.
  - Добавил премиальный всплывающий элемент-подсказку «Нажмите для взаимодействия с картой», исчезающий при клике.
- Восстановил исходный вид страниц `about.html` и `contacts.html`:
  - Полностью отменил изменения в `about.html` (revert к чистому состоянию).
  - Сбросил изменения формы и баннера в `contacts.html`, чтобы структура и стили блока `#contact-details` в точности соответствовали оригинальной версии из проекта `Stitch_first`.
  - Успешно запустил локальный веб-сервер на localhost (порт 3000).
- Устранил конфликт анимаций выпадающего меню на странице автопарка (`logistics.html`), ограничив область применения селектора `.is-visible` до `section.is-visible`, чтобы анимация меню происходила с той же скоростью и плавностью, что и на остальных страницах.
- Добавил обработчик клика на кнопку «ПОСМОТРЕТЬ ТЕХНИКУ» на странице автопарка, который плавно прокручивает страницу к блоку «СПЕЦТЕХНИКА ПАРКА» (`#fleet-hero-target`).
- Полностью скопировал оригинальный файл `contacts.html` из исходного проекта `Stitch_first` в рабочий каталог, чтобы восстановить точное отображение страницы контактов после Hero секции (включая оригинальные цвета Tailwind и отсутствие `theme.js`, которые восстанавливают видимость рамок полей ввода и розовый цвет акцентов).
- Реализовал эффект размытия (blur 6px) и затемнения/засветления для всех карт Яндекс (contacts.html, about.html, logistics.html), который плавно исчезает при клике/активации карты и возвращается при уходе курсора (mouseleave) или клике вне карты. Для этого удалил конфликтующие inline-стили фильтров и Tailwind-классы со страниц.
- Починил кнопку прокрутки наверх (scroll-to-top button) на всех страницах: убрал конфликтующие классы Tailwind `pointer-events-none`, `opacity-0`, `translate-y-10` непосредственно из HTML-разметки кнопки в `shared-ui.js` и `shared-ui-copy.js`. Вместо этого перенес управление интерактивностью и видимостью в CSS с помощью `pointer-events: none` / `pointer-events: auto` в селекторах по умолчанию и при наличии класса `.visible`.
- Настроил премиальный вид и отличный контраст для баннера срочной консультации в контактах (`contacts.html`) при светлой теме: изменил цвет белого текста заголовка («Нужна срочная консультация?») и цвет фона кнопки («ПРЯМАЯ ЛИНИЯ») на фирменный винно-красный цвет (`#964551`) in `shared-ui.js` and `shared-ui-copy.js` (сделав при этом иконку и текст внутри кнопки контрастными белыми).
- Заменил цвет слова «ДРОВОСЕК» в логотипе (в шапке, подвале и мобильном меню) на премиальный ярко-розовый цвет (`#d6336c`, соответствующий скриншоту 2) при активной светлой теме. Изменения внесены в динамические стили `lightModeStyles` файлов `js/shared-ui.js` и `js/shared-ui-copy.js`.
- Изменил цвет текста описания по умолчанию в выпадающем меню («Наведите на категорию, чтобы увидеть подкатегории» / `.mega-default-desc`) и заголовка (`.mega-default-title`) на черно-серый (`#3B3B3B`) при активной светлой теме в `js/shared-ui.js` и `js/shared-ui-copy.js` для обеспечения высокого контраста и читаемости.
- Добавил премиальную интерактивную анимацию наведения на логотип «ЖЕЛЕЗНЫЙ ДРОВОСЕК» во всех местах его использования (шапка, подвал, мобильное меню) в файлах `js/shared-ui.js` и `js/shared-ui-copy.js`. Анимация включает масштабирование, поворот и свечение изображения щита, а также плавный сдвиг с увеличением межбуквенного расстояния и неоновым свечением для надписи. Настройки адаптированы для темной и светлой тем.
- Настроил полупрозрачное оформление активных вкладок на светлой теме в соответствии с дизайном темной темы, заменив сплошную розовую заливку на полупрозрачный фон винно-красного цвета из скриншота 2 (15% непрозрачности `#964551` / `rgba(150, 69, 81, 0.15)`), а также обновив цвет текста и границы активной вкладки на `#964551` в файлах `js/theme.js`, `js/shared-ui.js` и `js/shared-ui-copy.js`.
- Исправил контрастность текста и иконок на кнопках с темно-бордовым (винным) фоном (`#964551`) при активной светлой теме:
  - Добавил CSS-правила в `js/shared-ui.js` и `js/shared-ui-copy.js` (в секции `lightModeStyles`), которые форсируют светло-серый цвет (`#c7c5c5`) для текста и иконок у всех таких кнопок (например, «ПАНЕЛЬ УПРАВЛЕНИЯ», «В КОРЗИНУ», отправка форм и т.д.) взамен темно-серого цвета `#3B3B3B`, вызывавшего конфликт контрастов.
  - Настроил интерактивное изменение цвета иконки и текста плавающей кнопки чата (`#floatingChatBtnGlobal`) при наведении на светлой теме: при hover-состоянии (когда фон кнопки меняется на белый) контент плавно окрашивается в темно-серый цвет (`#3B3B3B`) для отличной читаемости.
  - Настроил отображение стрелочки на кнопке прокрутки наверх (`#scrollTopBtnGlobal`) при наведении, чтобы при окрашивании кнопки в бордовый цвет иконка также принимала контрастный светло-серый цвет (`#c7c5c5`).
- Настроил оформление карточек популярных категорий в светлой теме для соответствия темной теме:
  - Убрал эффект размытия (blur) и серо-прозрачный фон с карточек, сделав подложку прозрачной для четкости фонового изображения.
  - Заменил сплошную заливку на градиентное засветление снизу (плавный переход от белого с 95% непрозрачностью к прозрачному), что обеспечило высокую читаемость текста.
  - Скрыл описание карточек по умолчанию на экранах шире мобильных (от 768px) и настроил его плавное появление с анимацией исключительно при наведении (hover).
- Добавил премиальную интерактивную анимацию при наведении на карточки заказов и обращений в личном кабинете (`cabinet.html`):
  - Добавил класс `cabinet-card` к шаблонам динамического рендеринга карточек заказов (`renderOrdersUI`) и обращений (`renderLeadsUI`).
  - Реализовал плавный подъем карточки при наведении (`translateY(-3px)`), изменение цвета рамки и мягкую тень с полупрозрачным розовым свечением.
  - Добавил эффект радиального градиентного свечения (`radial-gradient`) с полупрозрачным розовым цветом в качестве фоновой подложки (используется `#d6336c` для светлой темы и `#ffb0cc` для темной темы) с плавным проявлением (`opacity`).


