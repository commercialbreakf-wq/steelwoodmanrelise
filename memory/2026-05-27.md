# 2026-05-27

- Started the local backend server via `npm run dev` (listening on port 3000 with nodemon auto-reload).
- Fixed Material Symbols Outlined icons FOUT (Flash of Unstyled Text) bug where ligatures were displayed as text during page load. Added a CSS rule under `html:not(.material-symbols-loaded)` to keep unstyled text hidden and inside a 1em box, and added font loading detection using the `document.fonts.load` API.
- Implemented darker/more saturated hover state styling for unread notifications items in the chat list and active/hover state for chat folder button tabs (using the dark wine color `#964551` on hover instead of default white/pink).
- Configured Yandex SMTP (`smtp.yandex.ru:465`) using the credentials `info@steelwoodman.ru` / `ftknbbbwsxjxaniv`.
- Implemented a custom email registration confirmation workflow that bypasses Supabase's default SMTP. Added secure verification JWT tokens, custom verification emails via Yandex SMTP, and a landing route `GET /auth/confirm-email` to activate user profiles.
- Configured custom password reset email flows containing both direct reset links (using token hash for frontend compatibility) and 6-digit backup codes.
- Added automated order confirmation receipts sent directly to customers on successful checkout.
- Configured newsletter subscriptions (saving to `subscribers` table in Supabase and sending welcome emails with a discount promo code `WOODMAN5`).
- Validated all backend endpoints using `test_backend.js` (all tests successfully passed).
- Deployed the optimized codebase to the production Reg.ru server (`31.31.197.6`) using SCP and SSH extraction.
- Updated the production `.env` configuration file on the remote server to include valid Supabase keys and API credentials, restoring database warming, product loading, and authentication systems.
- Fixed a SyntaxError caused by the legacy Node.js version (`v10.24.0`) executing the backend process by setting the PM2 interpreter path to Node.js `v20.20.2`. Verified that database caching successfully loads 227 products and login endpoints respond correctly.
- Redesigned the admin panel left sidebar layout to make it compact (reducing padding, vertical spacing, and nav item heights) so it fits completely on the viewport without scrolling. Removed the Yandex Maps button and integrated the premium theme switcher toggle linked to the global `toggleThemeGlobal` API.
- Configured Analytics view interactive KPI cards: Выручка, Прибыль, Средний чек, and В работе are now clickable and link to beautiful detail list popups and financial dashboards. Exposed `showActiveOrdersModal` and `showRevenueModal` globally via `dashboard.js`. Fixed revenue dynamics chart grouping logic to align with calendar dates rather than sliding 24-hour windows. Fixed dynamics chart bar HTML template markup so columns render correctly.
- Fixed template literal variable interpolation escaping issue where backslashes prevented values (such as currency and dates) from evaluating inside popups and hover tooltips.
- Implemented deep drill-down popups on Revenue Dynamics chart columns and Top Revenue Drivers product items: clicking a bar opens a details modal with all orders for that calendar day, and clicking a top product driver card opens a modal showing all transactions containing that product. Both lists display quantities, prices, dates, and link directly to the order chat/details drawer.
- Added a responsive mobile folder switcher tab bar ("Заказы" / "Обращения") in the support chat modal header/dropdown section, complete with automatic active/inactive styling (using `#ca7093` and wine `#964551` color tones) and real-time badge notification counts synchronized between desktop and mobile layouts.
- Started the local development server on http://localhost:3000 to resolve the connection/frame origin mismatch error (`chrome-error://chromewebdata/`).
- Added the `whitespace-nowrap` class to the total checkout sum element (`#cartTotalPrice` in `cart.html`, `#totalPrice` in `product.html`, and `product-demo.html`) to prevent the currency symbol "₽" from wrapping onto a new line, ensuring the entire price is always displayed in a single line.
- Fixed a ReferenceError (`ADMIN_EMAILS is not defined`) in `api/[...slug].js` during support lead/appeal creation (`POST /api/leads` and `POST /api/orders`) by parsing the environment variable `process.env.ADMIN_EMAILS` and defining a global `ADMIN_EMAILS` array.
- Fixed a parameter syntax error (`-RecurRecurse` to `-Recurse`) in `pack_project.ps1`.
- Built the deployment package `deploy.zip`, uploaded it to the remote Reg.ru hosting server (`31.31.197.6`), extracted it, and restarted the PM2 service. Verified successful database warming and error-free execution.
- Fixed theme switcher toggle styling in the administrator panel sidebar. Since the CSS styles for the Apple-style toggle container, thumb, and icons were only injected when not on an admin page (in the `!isAdmin` conditional check in `js/shared-ui.js`), the toggle was rendering unstyled (vertical misaligned icons) in the admin panel footer. Copied the toggle's CSS selectors directly into the admin page's injected style block inside `js/shared-ui.js`, `js/shared-ui-copy.js`, and `temp_served.js`.
