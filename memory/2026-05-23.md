# 2026-05-23

- Исправлен баг, из-за которого в светлой теме все поп-апы (модальные окна) отображались в темных цветах.
  - Причина заключалась в синтаксических ошибках в селекторах CSS-переопределений светлой темы в `js/theme.js` (были не экранированы косые черты `/` в классах Tailwind, таких как `.bg-primary/10`, `.bg-primary/20`, `.bg-white/5`, `.bg-white/10`, `.qty-selector-container .bg-white/5`, `.calc-input-container .bg-white/[0.03]` и др.). Из-за этого браузер отбрасывал всё правило со стилями светлой темы для панели `.liquid-glass`, `.glass-panel` и выдвижных панелей.
  - Полностью экранировал все спецсимволы в селекторах CSS в `js/theme.js` (`.bg-primary\/10`, `.bg-primary\/20` и т.д.).
- Добавил стили для `.liquid-glass` светлой темы напрямую в инжектируемые стили `js/shared-ui.js` и `js/shared-ui-copy.js`, гарантируя белый ликвид-гласс фон с мягким размытием, тонкими границами и темным текстом.
- Настроил премиальный размытый полупрозрачный фон подложки (backdrops/overlays) для всех модальных окон (чат поддержки, обратная связь, карточки товаров, мобильные меню) при активной светлой теме (`background-color: rgba(21, 19, 17, 0.45) !important`), чтобы они выглядели гармонично и стильно, без глухого темного затемнения экрана.
- Отрегулировал стилизацию полей ввода и текстовых зон внутри `.liquid-glass` панелей для светлой темы: теперь сохраняется аккуратный стиль с границей только снизу (без принудительного заключения в серые прямоугольные рамки).
- Добавил переопределение фона боковой панели списка чатов `bg-[#0a0908]` и контактной карточки `bg-[#1a1816]` на `about.html`, чтобы они отображались светлыми в светлой теме.
- Устранил жестко зашитый темный фон успеха `successModal` в `reset-password.html`, переведя его на адаптивные переменные темы.
- Отключил анимацию корзины при наведении на карточку товара (в светлой теме). Теперь анимация корзины срабатывает только при непосредственном наведении на саму иконку корзины, а не при наведении на любую область карточки товара (удален селектор `.group:hover .pink-glow` из правил в `js/theme.js`). При наведении на карточку меняется только цвет самой иконки корзины на розовый (добавлен селектор `.group:hover .catalog-cart-btn`).
- Сделал текст заголовка и содержимого в кастомных модальных окнах `window.alert` более контрастным/темным в светлой теме (заменил жестко зашитые светлые цвета на Tailwind-классы `text-on-surface` и `text-on-surface-variant`).
- Внедрил защиту от дублирования асинхронных уведомлений в `window.alert` (с помощью глобального `Set` открытых сообщений). Теперь при одновременном вызове нескольких одинаковых предупреждений (например, при превышении лимита в 1000 тонн) отображается ровно одно окно.

- Исправил баг с отображением формы обратной связи в светлой теме на `about.html`:
  - Ранее в CSS-переопределениях `js/theme.js` и `js/shared-ui.js` символ решетки `#` в селекторах классов Tailwind с произвольными цветами (например, `.bg-[\#151311]`, `.bg-[\#0f0e0c]`, `.bg-[\#0a0908]`, `.bg-[\#ff4a7a]`) не был должным образом экранирован как `\\#`. Кроме того, при первой замене были ошибочно заменены двойные обратные слэши перед квадратными скобками на одинарные (что ломало экранирование скобок в JS-строке). Из-за этого браузер отбрасывал всю группу CSS-правил для светлой темы.
  - Полностью восстановил правильное двойное экранирование спецсимволов (`\\[\\#...\\\]`) во всех селекторах в `js/theme.js`, `js/shared-ui.js` и `js/shared-ui-copy.js`.
  - Добавил класс `.bg-[\#1a1816]` (контейнер формы обратной связи на `about.html`) в список CSS-переопределений светлой темы в `js/theme.js`.
  - Увеличил версию подключения `js/theme.js` с `?v=1` до `?v=2` во всех HTML-файлах проекта для сброса кэша браузера у пользователя. Теперь при включении светлой темы форма корректно перекрашивается в премиальный светлый стеклянный (glassmorphism) стиль с темным текстом.
  - Дополнительно перевёл разметку формы в [about.html](file:///c:/Users/egas1/Downloads/Stitch_first%20%E2%80%94%20%D0%BA%D0%BE%D0%BF%D0%B8%D1%8F%20%283%29%20%E2%80%94%20%D0%BA%D0%BE%D0%BF%D0%B8%D1%8F/about.html) на нативные классы Tailwind для светлой/тёмной темы (`bg-white dark:bg-[#1a1816]`, `border-black/10 dark:border-white/5` и т.д.). Это гарантирует 100% корректную работу темы для формы обратной связи даже при локальном открытии через протокол `file://` (без запуска веб-сервера), когда внешние JS-скрипты не могут загрузиться из-за абсолютных путей.
- Исправлено отображение экрана загрузки (прелоадера) в светлой теме:
  - Ранее в CSS-правилах светлой темы в `js/shared-ui.js` и `js/shared-ui-copy.js` по ошибке использовался селектор `.global-loader-container` вместо ID самого элемента прелоадера `#globalPreloader`. Из-за этого прелоадер в светлой теме оставался тёмным.
  - Исправил селектор на `html.light #globalPreloader, html:not(.dark) #globalPreloader`. Теперь при загрузке страниц в светлой теме отображается эстетичный светлый полупрозрачный фон с эффектом размытия (frosted liquid glass blur), контрастным темным текстом и розово-малиновым индикатором прогресса.
- Изменил оформление кнопки переключения темы (Apple-style theme toggle):
  - Настроил адаптивные фоновые цвета: в темной теме кнопка имеет матово-темный черно-серый оттенок (подложка `#1c1a18`, ползунок `#2d2a28`), а в светлой теме — стильный белый/светло-серый вид (подложка `#EBEBEB`, ползунок `#FFFFFF`) в файлах `js/shared-ui.js` и `js/shared-ui-copy.js`.
  - Изменил цвет активной иконки внутри кнопки (луна/солнце) с желтого/розового на постоянный стильный розовый цвет `#ca7093` для обеих тем.
  - Добавил элегантное свечение и границы розового цвета при наведении на ползунок переключателя (`border-color: #ca7093`, `box-shadow` с розовым размытием) in `js/shared-ui.js`, `js/shared-ui-copy.js` and `js/theme.js`.
  - Увеличил версию подключения `js/shared-ui.js` до `?v=14` во всех HTML-файлах для сброса кэша и мгновенного обновления у клиентов.

- Исправлен баг, из-за которого форма технической поддержки (чат-окно) закрывалась при любом клике внутри нее.
  - Причина заключалась в том, что контейнер модального окна `globalChatDrawerModal` имел класс `pointer-events-none`, а сама панель чата `#globalChatModalPanel` для авторизованных пользователей не имела класса `pointer-events-auto`. Из-за этого клики по элементам чата (кнопкам, текстовым полям) проходили насквозь и попадали по фоновой подложке (overlay/backdrop), закрывая форму.
  - Добавил класс `pointer-events-auto` к элементу `#globalChatModalPanel` в `js/shared-ui.js` и `js/shared-ui-copy.js`.
  - Увеличил версию подключения `js/shared-ui.js` до `?v=15` во всех HTML-файлах для сброса кэша у клиентов.
